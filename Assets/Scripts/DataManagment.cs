using System.Collections;
using System.Collections.Generic;
using System.Drawing;
using UnityEngine;

public class DataManagment : MonoBehaviour
{
    //SearchParameters search = new SearchParameters();
    
    //public void initializeSizeGrid(int size)
    //{
    //    search.grid = new int[size, size];
    //    search.size = size;
    //}
    //public void setGridData(int x, int y, int number) => search.grid[x, y] = number;
    //public int getGridData(int x, int y) => search.grid[x, y];

    //public bool getNeighavalaibility(int x, int y) => search.checkAvalaibility(x, y); 

    //public List<Node> searchA(int startX, int startY, int endX, int endY)
    //{
    //    search.startLocation = new Node(new Point(startX, startY));
    //    search.endLocation = new Node(new Point(endX, endY));

    //    return search.aStar();
    //}
}
